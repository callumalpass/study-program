import { Project } from '../../../core/types';

// Math203 (Calculus I) uses exams instead of projects for assessment.
// This is consistent with the assessment strategy for math subjects,
// where exams better test conceptual understanding, calculation skills,
// and problem-solving abilities.

export const math203Projects: Project[] = [];
